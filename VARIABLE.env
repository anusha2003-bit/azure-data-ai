azure_oai_endpoint = os.getenv("AZURE_OAI_ENDPOINT")
        azure_oai_key = os.getenv("AZURE_OAI_KEY")
        azure_oai_deployment = os.getenv("AZURE_OAI_DEPLOYMENT")
        azure_search_endpoint = os.getenv("AZURE_SEARCH_ENDPOINT")
        azure_search_key = os.getenv("AZURE_SEARCH_KEY")
        azure_search_index = os.getenv("AZURE_SEARCH_INDEX")
/*

azure_oai_key = os.getenv("AZURE_OAI_KEY"):

This line gets the value of the environment variable AZURE_OAI_KEY and stores it in the variable azure_oai_key. This key is likely used to authenticate requests to the Azure OpenAI service.
azure_oai_deployment = os.getenv("AZURE_OAI_DEPLOYMENT"):

This retrieves the deployment identifier for the Azure OpenAI model and stores it in azure_oai_deployment. It specifies which model version or instance to use.
azure_search_endpoint = os.getenv("AZURE_SEARCH_ENDPOINT"):

This retrieves the endpoint URL for Azure Search, which is used to query and interact with your search service.
azure_search_key = os.getenv("AZURE_SEARCH_KEY"):

This line fetches the API key for authenticating requests to the Azure Search service, storing it in azure_search_key.
azure_search_index = os.getenv("AZURE_SEARCH_INDEX"):

This retrieves the name of the search index in Azure Search, which is where your searchable data is stored, and stores it in azure_search_index.

*/
